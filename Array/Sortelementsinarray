#include <bits/stdc++.h>
#include<cmath>
#include<vector>
using namespace std;

int main()
{
    int arr[] = {5,4,1,2,3};
    int n = sizeof(arr)/sizeof(arr[0]);
    sort(arr, arr+n);
    for(auto i:arr)
    cout<<i<<" ";

    return 0;
}
